- name : vars from args
  hosts: localhost
  connection: local
  tasks:
    - name: print vars from args
      ansible.builtin.debug:
        msg: "The name is {{ name }} and the version is {{ version }}"